{% comment %}

This snippet renders a product into a product list.
By default, it's only used by the product-list.liquid snippet.

{% endcomment %}
<div class="product-grid-wrapper">
  <a href="{{ product.url | within: collection }}" title="{{ product.title }}" class="product-grid">
    <div class="product-image-wrapper">
      <div class="product-image square fades-in">
        <img src="{{ product.images.first | product_img_url: 'original' }}" alt="{{ product.title }}" onload="backgroundLoaded(this)"/>
      </div>
    </div>
    <h5 class="product-title">
      {{ product.title }}
      {% if product.compare_at_price_max > product.price and settings.product_show_sale_tag %}
      <span class="label label-success">SALE</span>
      {% endif %}
    </h5>
  </a>
</div>